[variables]
NODE_VERSION = "20"

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter @workspace/db build", "pnpm --filter @workspace/api-client-react build", "pnpm --filter @workspace/corneriq build"]

[start]
cmd = "npx serve dist -p $PORT"
